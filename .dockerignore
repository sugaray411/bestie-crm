# .env holds live database and API credentials and must never be baked into an
# image layer -- layers are readable by anyone who can pull the image.
.env
.env.*
!.env.example

node_modules
dist
.git
.gitignore

tests
vitest.config.ts

*.log
npm-debug.log*
